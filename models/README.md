# Models Directory

Trained model artifacts are written here by the CLI:

```powershell
python -m energy_forecasting.cli train --data data/raw/CA2-Energy-Consumption-Data.csv
```

Model binaries are ignored by Git because they are generated artifacts and may
depend on private data.
