# Data Directory

Place the coursework CSV here:

```text
data/raw/CA2-Energy-Consumption-Data.csv
```

Generated or cleaned data can be written to `data/processed/`.

Raw and processed datasets are ignored by Git by default. Keep this README and
the `.gitkeep` placeholders so the folder layout remains visible.
