# Source Code

This folder contains the installable Python package for the project.

The package is kept separate from notebooks so reusable logic can be linted,
tested, imported by scripts, and used by CI.
